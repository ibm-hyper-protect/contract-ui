# Electron Builder Environment Variables
# These settings optimize the build process

# Skip code signing for faster builds (enable for production releases)
CSC_IDENTITY_AUTO_DISCOVERY=true

# Use faster compression
ELECTRON_BUILDER_COMPRESSION_LEVEL=normal

# Parallel builds
ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES=true
